import type { SceneProps } from "../router";
import { PlaceholderCard } from "./PlaceholderCard";

export default function ListPlaceholder(_props: SceneProps) {
  return <PlaceholderCard station="list" />;
}
