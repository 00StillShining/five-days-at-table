import type { SceneProps } from "../router";
import { PlaceholderCard } from "./PlaceholderCard";

export default function TodayPlaceholder(_props: SceneProps) {
  return <PlaceholderCard station="today" />;
}
