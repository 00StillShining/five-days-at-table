import type { SceneProps } from "../router";
import { PlaceholderCard } from "./PlaceholderCard";

export default function PlanPlaceholder(_props: SceneProps) {
  return <PlaceholderCard station="plan" />;
}
