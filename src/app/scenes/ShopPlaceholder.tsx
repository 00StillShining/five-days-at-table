import type { SceneProps } from "../router";
import { PlaceholderCard } from "./PlaceholderCard";

export default function ShopPlaceholder(_props: SceneProps) {
  return <PlaceholderCard station="shop" />;
}
