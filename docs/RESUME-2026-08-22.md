# RESUME — Saturday 2026-08-22

*Written 2026-08-18 when development was halted at the owner's request. Self-contained:
read this, then `docs/BUILD-STATE.md`, then `git log --oneline`.*

## Read these first, in this order, before any styling code

1. **`docs/CD-BRIEF.md`** — the binding contract. Read **Measured performance law** and
   **Contrast-audit integrity** in particular; both were written mid-build and both
   overturn assumptions the original plan made.
2. **`/Users/stillshining/correctionary/CORRECTIONARY.md`** — the CONTROLLING AUTHORITY.
   Beats Council Defiance always. **It is NOT on the skill's path**, so invoking the
   council-defiance skill normally returns the revoked taste doctrine with no signal that
   it is revoked. This remains the single most likely cause of silent failure.
3. The approved plan: `~/.claude/plans/okay-now-lets-design-stateful-thompson.md`.

## Where the build stands

**Six of eight surfaces are LANDED and verified**, each with its gates checked
independently by the orchestrator rather than taken from the builder's report:

| surface | language | commit |
|---|---|---|
| foundation `src/cd/**` | — | `a85e57e` |
| STORES perf spike (gate, passed) | — | `f192758` |
| chassis | 13 BACK CHANNEL | `3ad2bc4` |
| TODAY | 05 EXPOSED WORKS | `9757763` |
| COOK | 02 REEL LOGIC | `da530ea` + `23782ee` |
| LIST | 20 IRREDUCIBLE | `53fc5df` |
| PLAN | 17 RESTOMOD | `227d935` |
| STORES | 10 CLEAR LID | `a48bd8d` |

**Two are UNFINISHED: MEAL (16 FACETED VOLUME) and SHOP (11 TANGENT HORIZON)**, saved as
WIP at `4619bf0`. Both builders were killed by a session limit mid-build. Neither
reported, so neither has a five-pass critique, a ladder artifact, measured verification,
or a §6.6 checklist.

## THE FIRST THING TO DO ON SATURDAY

**The tree is tsc-RED by exactly one error**, so `npm run build` fails. Tests are fine —
**674/674 pass**.

```
src/screens/shop/index.tsx(510,10): error TS2739
Type '{...}' is missing the following properties from type 'PlinthProps':
age, pricedOn, wasted, onRecover
```

`Plinth` gained four props; its call site passes none. **Three are already in scope at
the call site** — `sheetAge` (line 252), `pricedOn` (248), `wasteTotal` (409) — and the
`etch` string on the next line already uses all three. That part is mechanical.

**`onRecover` does not exist anywhere in the file, and it must not be faked.** It is the
freshness layer's required recovery action for a stale price sheet, at a fixed position.
A plausible no-op there produces a screen that looks finished and lies about being able
to recover — the exact failure class this build kept catching (COOK's alarm sounding for
a hypothesis; LIST's plan that always agreed with the till). **Resume the SHOP builder
and let it supply the real action.**

**Last fully green commit: `a48bd8d`.** Nothing between it and `4619bf0` is load-bearing
for the six landed screens, so resetting to it is safe if that is ever preferable.

## Then, in order

1. **Resume the MEAL and SHOP builders from their transcripts.** They were briefed in
   full; do not re-brief from scratch. Each still owes: the five-pass critique in
   defect–evidence–repair form, its ladder artifact (`docs/ladder/meal-ladder.html`,
   `docs/ladder/shop-ladder.html`), measured verification, the §6.6 checklist, and §6.5
   limitations. **The SHOP builder's last words were that it was about to fix an INVENTED
   `verifiedOn` value — treat any date on that screen as unverified until it reports.**
2. **Repair the foundry's six gradient surfaces** (`.cd-key`, `.cd-register-head`, the
   `Enclosure` variants). They use the `background` shorthand, which resets
   `background-color` to transparent — so every contrast walker that reads computed
   `backgroundColor` measures against an ancestor, not the painted ground. Deliberately
   deferred while builders were mid-flight. See CD-BRIEF's **Contrast-audit integrity**.
3. **Re-sweep every landed screen's contrast** using the composited gradient (worst =
   darkest stop). Several screens reported "0 contrast failures" with the naive walk;
   **those figures are suspect wherever text sits on a gradient surface.** STORES measured
   two inks at 4.98:1 that were really 3.63:1 and 3.91:1.
4. **Resolve the carried foundation items** (all recorded, none urgent):
   - `Voicing.contactOnRelease` in `src/cd/sound/cues.ts` has no consumer; `PressKey`
     still voices on the down-stroke. Either `PressKey` honours it or the flag goes.
   - `useTrophy.ts` is duplicated **four** times now (TODAY, COOK, PLAN, LIST) —
     promote to `src/cd/`.
   - A closed `Tray` at `inline-end` leaves the document programmatically scrollable by
     its own width (not user-scrollable; a `scrollIntoView` can expose the gutter).
   - `.cd-stale` declares `grid-template-columns` in `@layer cd.type`; a screen
     overriding `display` alone still inherits the four-track list, because a layer wins
     per **property**, not per rule.
   - `src/cd/material/textures.ts` cannot be re-keyed to a world's palette.
5. **Delete the legacy bridge.** `world.css` still carries two temporary selectors
   (`.fd5[data-language="playful"]` / `"precision-industrial"`) aliasing onto CLEAR LID
   and REEL LOGIC for screens that had not been rebuilt. Once MEAL and SHOP land, **no
   screen needs them** — remove them and the `--sol-*` bridge block.
6. **Deploy**: `npm run build && $HOME/.local/fd5-netlify/node_modules/.bin/netlify deploy --prod --dir dist`
   (folder already linked). Devices see a new deploy on their **second** visit — the
   service worker is versioned.
7. **Then the single Fable review**, over all eight surfaces as one product, walked
   screen-to-screen. **OWNER REVISION 2026-08-18: its findings ARE to be actioned**, not
   merely written up. Timing is unchanged — one review, at the very end, no mid-build
   design gates.

## Standing facts that are easy to get wrong

- **The dev server is UNCONDITIONALLY SILENT** (`src/cd/sound/bus.ts`). Owner asked twice.
  `readMute()` returns true in DEV before consulting anything and `setMuted` refuses to
  unmute there. **Production is unchanged.** Do not add an escape hatch — a sound audit
  is done by inspecting what the bus *schedules*, never by listening: *"A muted
  instrument is a complete instrument."*
- **Frozen:** `src/state/**`, `src/engine/**`, `src/data/**`, `tools/**`, `data/**`.
- **Amber line 2.0 MB.** Last green build was **1.460 MB**.
- **Executing week is ALWAYS "A"** (D5, "a-twice"); `prefs.week` is a browsing toggle only.
- **Verify builder reports; do not trust them.** This paid off repeatedly: three Floor
  failures the foundation builder had recorded in its own comments and shipped anyway; a
  chassis "net smaller" claim that was a 51 KB *increase* (MiB vs MB); a PLAN report of a
  malformed gradient that parse-checking disproved; and a PLAN report of a tsc break from
  a file that no longer existed.
- **Three traps that cost builders real time**: a **backgrounded tab does not advance
  transitions or rAF** (two builders nearly filed false defects); **`?raw` CSS imports
  return `""` under vitest**, so a stylesheet test can pass against nothing; and a
  **dynamic `import()` under the dev server gets its own module graph**, so probing
  singletons that way measures a different instance.

## Carried defect

**PLAN's 44px link defect is CLEARED** (`.pln-slot-name` now 161.8–264.5 × 44px). No
other carried screen defects outstanding.
