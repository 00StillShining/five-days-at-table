# CD-BRIEF — the binding design contract for FD-5

**Read this file first. Then read `/Users/stillshining/correctionary/CORRECTIONARY.md` in full. Then, and only then, the Council Defiance chapters your task names.**

## Why this file exists (read this part twice)

The Council Defiance skill installs at `~/.claude/skills/council-defiance/`. **THE CORRECTIONARY — which overrides it — is NOT in that folder.** A builder who invokes the skill normally receives the bible's taste doctrine fully intact, with no signal that most of it is revoked, and will build restrained, tasteful, "clean" work believing it correct. That build fails this project's gate.

> **The tell:** *"if you find yourself producing something quiet, flat, tasteful, restrained, or 'clean,' you have followed a revoked law. Stop. Re-read §2 and §3. Rebuild louder."*

If you cannot cite the CORRECTIONARY clause you built a decision from, you did not read it.

## Precedence

1. **CORRECTIONARY** — controlling authority. Every conflict, no synthesis, no averaging, no "honoring the spirit of both."
2. **Council Defiance** — the engineering substrate only: physics, material construction, foundry geometry, sound, honesty, the accessibility Floor, and the process ladder. Its **taste doctrine is revoked outright** and does not apply even where the CORRECTIONARY offers no replacement.
3. **This brief** — the project's own rulings, below. Where it resolves a conflict, that ruling is final for FD-5.

---

## CORRECTIONARY §3 — REVOKED laws (verbatim)

**3.1 — "Hierarchy concentrates craft" / one hero per screen — REVOKED.** Replaced by **uniform lavishness**: a dashboard carries **four to six simultaneous hero-grade instruments** as its normal condition — think a supercar's full cluster, where every dial is deep-drawn, not one jewel among plain gauges. Every element on screen gets full material treatment: real shadow stacks, real specular light, real weight. "Plain competence" as a deliberate design register is abolished. The tier system may still be used to *organize construction* — never to strip elements down to it.

**3.2 — The accent budget and neutral-field doctrine — REVOKED.** Replaced by **material richness as the ground**: carbon weave, brushed and machined metal, leather, glass, lacquer, and colored emissive glow are the *default surface of the screen*, not rare exceptions rationed onto one element. Spend color the way the anchors spend it — Koenigsegg's amber telemetry, Pagani's cognac leather against raw aluminum, Nothing's red accents — freely, wherever it serves the object. The semantic colors (warning, danger, success, live) keep their reserved meanings and their redundancy rules; that part is engineering, not taste.

**3.3 — Microtexture caps and quiet-surface rules — REVOKED.** Replaced by **visible, felt texture**: knurling you can almost grip, weave you can read, machined grain that catches light — at whatever strength reads as real material. The only test is *"does this read as the material it claims to be?"* — never *"is it quiet enough?"* The bible's texture recipes remain the correct **construction methods**; their intensity caps are gone.

**3.4 — The distance test, the never-copy bundles, and the anti-mimicry law — REVOKED.** Replaced by its inversion: **mimicry of real hardware is the goal.** A viewer naming the hardware family within five seconds is *success*, not failure. The only remaining bans, and they are hard bans: **no real manufacturer logos, wordmarks, or trademarks rendered in the UI, and no presenting a fictional product as a real one.** Everything short of that — silhouettes, materials, layouts, instrument grammars, the whole visual soul of the source hardware — is explicitly permitted and encouraged.

**3.5 — The 70/20/10 single-language dominance rule — LOOSENED.** Multiple languages may run at **full richness on one screen**, each owning complete instruments or zones. The one part that survives: **don't blend two languages inside a single control.** Give each language whole objects; never dilute one into a 20% garnish of another.

**3.6 — Book IV's composition ratios as law — DOWNGRADED to suggestions.** The 62/38 grids, card-stack evidence columns, and remove-not-shrink trophy math are available tools, not requirements. **Supercar-cluster and instrument-panel composition (§5) outranks card math wherever they disagree.**

**3.7 — The bible's authoring-voice rules — OUT OF SCOPE.** Banned-words lists, word budgets, lint gates, Book V review protocols. They govern how the bible's own chapters are written. **Ignore them entirely here.**

---

## CORRECTIONARY §5 — Composition orders (the anti-SaaS mandates)

1. **Build enclosures, not cards.** Bezels, faceplates, wells, escutcheons, rails, pods, clusters, housings. If any container is a rounded rectangle with padding, a border, and a soft shadow — remake it as machined housing before continuing. **The word "card" appearing in your own reasoning is a warning sign.**
2. **Compose like an instrument panel, not a layout.** Purposeful clusters with real relationships; dense zones where the action is; materials meeting each other the way they do in a cockpit. Not a uniform grid with even gutters and interchangeable slots.
3. **Every value is an instrument.** A needle over a printed arc, a drum counter, segmented digits, a bar of real LEDs, a lamp, a flap — **plus its exact figure. A number typeset on a background is not a readout and is not acceptable as one.**
4. **The ground is a material.** Carbon, brushed metal, leather, glass, or lacquer — with a light direction acting on it. **A flat hex-color page background is the enemy's ground.**
5. **Density is confidence.** Fill the panel the way a cockpit is filled. Empty space is a deliberate framing device used sparingly — not a default politeness applied everywhere.
6. **Light exists.** One strong consistent direction; real speculars on metal and glass edges; genuine glow from emissive elements (**glow means bloom, not just a bright fill**). If nothing visibly catches light, fix that before anything else.
7. **Controls look operable.** Knurled, seated in wells, visible travel and states. Mistakable for a static icon = failed. Not keyboard-operable = failed the Floor.
8. **Typography behaves like hardware type.** Engraved, silkscreened, printed, backlit, or segment-formed — belonging to a surface, **tabular figures wherever digits change.** Never floating UI text that belongs to no material.

## CORRECTIONARY §7 — The quick card (verbatim)

- **AUTHORITY:** This file beats Council Defiance. Every conflict, no exceptions, no blending.
- **KEEP:** physics · material construction (as floors, scale up freely) · foundry geometry · sound engine · honesty · accessibility floor.
- **REVOKED:** one hero per screen · accent budgets and neutral fields · texture caps · the distance test and all anti-mimicry law · single-language purity.
- **BUILD:** enclosures, not cards · instruments, not numbers · a material ground under real light · four-to-six heroes, all lavish · dense, weighted, dramatic, fun.
- **MIMIC:** real hardware, freely and closely. Only logos, wordmarks, and trademarks are off-limits.
- **CHECK:** with your eyes, against the vision, out loud, before "done."
- **THE TELL:** if it looks safe, you followed the wrong document.

## CORRECTIONARY §4 — RETAINED engineering (binding; intensities are FLOORS, scale up only)

- **Four mass classes:** Light `spring(0.7,420,25)` · Standard `spring(1,320,26)` · Weighted `spring(1.4,260,30)` · **Anchored `spring(1.2,300,38)` with 0% overshoot, always.** (Panel `spring(1.6,240,30)` is Weighted at chassis scale, not a fifth class.)
- Detents capture at **±40% of pitch**, **12% hysteresis**, **80–160ms seat**. New input **retargets any motion in flight**.
- Four-layer shadow stack (rim-light / contact / core / ambient), specular hard-stop pairs, brushed-metal anisotropic banding, press compression **≤20ms linear down-stroke, 80ms settle release**.
- **Guarded switch** (hold-to-arm with visible linear fill) for anything consequential.
- **Sound:** one master bus, five cues (contact/detent/latch/confirm/warning), detents coalesced above 8/s, tritone reserved for genuine alerts, **total silence until the user's first real gesture**.
- **Honesty — non-negotiable:** every live reading shows its **exact figure and its age**; a value that stops updating **declares itself stale**; no invented data, no fake progress, **no motion with nothing behind it. "Drama never lies."**
- **The Floor — non-negotiable:** **44px targets both axes · 4.5:1 text · 3:1 control/focus · keyboard-operable · `prefers-reduced-motion` respected with meaning preserved · 60fps sustained · input acknowledged within 16ms.** *"Text sits on ink plates, dial faces, and engraved panels within the material world, not raw across carbon weave. Meet the floor inside the richness, never by retreating to flatness."*

## CORRECTIONARY §6.6 — The exit checklist (answer every line explicitly in your report)

- Enclosures and instruments — zero SaaS cards, zero bare-number "readouts"?
- Material ground with a real light direction?
- Four or more hero-grade instruments, all at full treatment?
- Every control physically alive — operable, weighted, keyboard-accessible?
- Honesty intact — real values, ages, staleness, no fake motion?
- Floor intact — targets, contrast, keyboard, reduced-motion?
- The §2 test — would a viewer's first words be about an *object*?

**A "no" on any line means the build is not done.** Also binding: **§6.2 — render it and look at it.** *"Code that 'should look right' is worthless testimony."* Never report a visual build finished without having viewed it. **§6.5 — no silent downgrades:** if a requirement cannot be met, say so plainly; never quietly build the simplified version and present it as the ask.

---

# FD-5 project rulings (final for this build)

## Language assignment — one per screen, plus the chassis

| Surface | Language | Chapter |
|---|---|---|
| Chassis / rail | **13 BACK CHANNEL** | `languages/13-glyph-matrix.md` |
| TODAY | **05 EXPOSED WORKS** | `languages/05-pagani-utopia.md` |
| PLAN | **17 RESTOMOD** | `languages/17-singer-911.md` |
| MEAL (tray) | **16 FACETED VOLUME** | `languages/16-ak-sp3000.md` |
| COOK | **02 REEL LOGIC** | `languages/02-tp7.md` |
| STORES | **10 CLEAR LID** | `languages/10-braun-sk4.md` |
| SHOP | **11 TANGENT HORIZON** | `languages/11-beogram-4000.md` |
| LIST | **20 IRREDUCIBLE** | `languages/20-presto-dl400.md` |

Zone fence: `data-cd-language="<slug>"` on each scene root. **No element may read tokens from two language scopes.** Read your chapter **in full** — §2 world rules, §3 component treatments, §4 archetypes, §5 physics, §6 motion/sound, §9 failure modes. Cite the values you use.

## Product-wide rulings (override any chapter that disagrees)

1. **Needle slew = 720°/s project-wide** (ch.05's declared delta, not the doctrine's 540°/s): full 270° arc in 375ms, floored at 80ms. Rate, never a fixed-duration tween.
2. **One sun at 138°**, shadow ratio 1:2, every screen.
3. **The five reserved roles are one hex each product-wide** — focus, live, warning, danger, success. Semantic colour is engineering (§3.2), not taste. **`live` = `#FF2B1A`** (REEL LOGIC's red); the chassis rec-dot uses the same hex, so "a cook program is running" is one claim made once. TANGENT HORIZON's index red `#C8272B` is fenced to SHOP and means *a position that has stopped*, never live. RESTOMOD's needle orange `#E8590C` is livery for needles only — never a fill or lamp.
4. **The Refined rung is re-authored.** CD's stock rung ("one hierarchy", "plain competence below") is revoked by §3.1 and would systematically undo the push. **New rung: subtract jobless elements; keep every survivor at full material strength.** The ledger asks *does it state a value, or let the hand do something?* — never *is it the hero?* Exit: 4–6 hero-grade instruments survive, each with a job named in one sentence.
5. **The ink-plate law.** Text never renders raw over carbon weave or brushed grain. It sits on a cream face, an anthracite panel, or an engraved billet plate. **Text-on-carbon is a hard fail by rule, not by measured ratio.**
6. **Ornament without function is the structural risk of this product.** FD-5's data is slow — most values change once a day. **Portholes, rocker arms and spinning elements attach only to values whose change is user-caused or clock-continuous.** A daily stock level gets a needle and a printed zone, not a spinning disc. Stable data, stable instrument.
7. **Procedural materials only.** Canvas textures painted once and cached by `{pattern, scale, dpr}`; no image assets; **WebGL is banned** (single-file build + offline). Build stays under a 2.0 MB amber line.

## Measured contrast repairs (already computed — use these)

1. Warning `#C4832A` on cream `#EDE6D6` = **2.55:1**, fails. Use **`#8D5E1E` (4.50:1)** for warning *text*; `#C4832A` stays legal only as a wide printed zone band.
2. Macro channel text is **5.17–5.39:1 on cream** but **1.69–2.30:1 on carbon/anthracite**. **Channel-coloured text renders only on cream faces.** Elsewhere channel identity = position + engraved label + segment fill. Lifted set where unavoidable on anthracite: protein `#FF7F47` · fat `#C99F00` · carb `#75A3FF` · fibre `#00BD66`.
3. **No single focus hue clears 3:1 on satin billet.** Focus ring is **two-tone: 2px `#4C8FD1` + 1px `#141414` keyline, offset 3px** — verified ≥3.33:1 on all seven grounds.

## Structure rulings (owner, this round)

- **R5 — No cage anywhere.** D6 (COOK lid-closed) is superseded: the rail is present on **every** screen; the user may switch screens at any time.
- **R6 — Open panels survive navigation, reset on reload.** Panel state lives in a chassis-level context keyed by screen (scenes unmount on navigation), **not** in screen-local `useState`, and **not** in the frozen store.
- **R7 — No page-length scrolling.** Long registers use a **grouped accordion, one section open at a time**, with the hidden remainder printed as a number ("confess the overflow"). Only one group renders at full material at a time.
- **MEAL is a tray** over the screen that summoned it (PLAN or TODAY), never over COOK, still URL-addressable. **LIST stays its own room** (it arrives cold from a QR scan on another device).
- Trays travel their own size with **no scrim** — the workspace beneath stays live and touchable.

## Frozen — do not modify

`src/state/**` · `src/engine/**` · `src/data/**` · `tools/**` · `data/**`. All **358 tests must stay green**. The plan-variant system (`full` | `morrisons-tester`) must survive: every screen keeps consuming `activeVariant(state)`; only how a cut slot *looks* changes.

## Self-critique — mandatory, before every handoff

There are **no intermediate design reviews on this build**. One Fable review runs at the very end, over the whole product, and its findings are planned rather than applied. That makes your own critique the only critique your work receives before it lands, so run the full protocol, not a glance.

**Run the five passes in fixed order — truth → hierarchy → physics → craft → character.**

| Pass | The question | Fails when |
|---|---|---|
| **Truth** | Does every rendered value report real state? | Invented numbers, smoothed reversals, progress theater, a stale reading without its age, motion with nothing behind it |
| **Hierarchy** | Does the reading order serve the frame's task? | The operator's eye lands somewhere the task doesn't need. *(Note: CD's "exactly one element leads" is **revoked** by CORRECTIONARY §3.1 — four-to-six heroes is the normal condition. Judge reading order, not hero count.)* |
| **Physics** | Does mass track consequence, and does the commit land at input? | Animation delaying the semantic commit; an Anchored control that overshoots; a needle that jumps instead of sweeping |
| **Craft** | Is every value committed and every recipe executed as written? | A shadow layer with no role; a gradient without stops; a duration that exists only as a feeling |
| **Character** | Would the anchor companies ship this? | Anonymous competence; it reads as an app before it reads as an object |

**The halt rule:** a pass that fails receives findings for that pass alone and **the critique stops there** — "a craft note on a lying gauge polishes the lie." Repair, then **re-run from truth**, because repairs regress.

**Write findings before you change anything**, in defect–evidence–repair form, with committed values in the repair and no taste-words:

```
DEFECT    Needle smoothing hides a threshold crossing (truth pass)
EVIDENCE  Stock level 0→1 at 14:32:07 renders as a 300ms ease; the warning lamp fires 280ms late
REPAIR    Bypass the 120ms display smoothing for any excursion past a zone boundary
```

**Then run these tests** (the distance test is revoked by §3.4 — the §2 acceptance test replaces it):
- **Static equivalence** — freeze all motion, mute all sound, desaturate to grey. The task must still complete. Motion, sound and colour are reinforcement channels only.
- **Squint / 8px blur** — the reading order must survive.
- **Real data** — the longest ingredient name, the 65-row register, the 49-line basket, the un-anchored fortnight, the tester variant, an expired item, an empty state.
- **Hundredth use** — perform the core loop once attentively, then as the hundredth repetition of the day. Every motion the operator must wait through fails.
- **§2 acceptance** — would a viewer's first words name an *object*, or a software category?

> **"An agent that reports zero findings on a first build has not critiqued. It has glanced."** A report with an empty findings list will be returned.

## Measured performance law (STORES spike, 2026-08-17 — binding, not advisory)

The spike built one STORES accordion group at full material against the real 65-row
register and measured it three ways. **Its findings overturn the assumption the plan was
built on, so build from these numbers, not from instinct.**

**Full material is NOT the bottleneck, and you may stop economising on it.** Four-layer
shadow stacks, specular, procedural texture and live acrylic at 65 rows cost **under 1ms
of style+layout per frame even at 20× CPU throttle** — 0.9% of the frame. Zero Long
Animation Frames at the mobile-throttled baseline in *every* configuration tested,
including a deliberately naive translation with 828 shadow layers, 65 `backdrop-filter`
panes and 66 blend layers. 60fps held throughout. **Richness is affordable. Spend it.**

**What actually costs frames is React reconciling the register — 97% of the frame is
JavaScript.** Budget renders, not layers.

Therefore, binding for every screen from here:

1. **Commit synchronously at input; do not rely on a React dispatch.** A `dispatch` alone
   does **not** satisfy the Floor's 16ms acknowledgement, because the reducer runs during
   the *next render* — the model is still stale when your handler returns. Keep the model
   in a ref, apply the frozen reducer to it **inline at the input event**, and let
   `setState` be only the request to re-render. Measured: ack commit **0.01–0.9ms** while
   the visible report takes 12–39ms. This is the Floor made structural; promote it first.
2. **Unmount closed accordion groups.** This was not in the plan and it is the single
   largest saving: 767 → 335 nodes, render 19.4 → 12.5ms, input→paint 39.0 → 17.6ms.
   **R7 alone is insufficient** — a collapsed drawer at `grid-template-rows: 0fr` is
   hidden from paint but *not* from React, so one level change still reconciles all 65
   rows. Measure the accordion on the real device before generalising: at desk width this
   trades stocktake cost for group-switch cost.
3. **One cached texture per family is non-negotiable** — 65 requests on one key cost
   4.0ms/1 paint; on 65 keys, 341.0ms/64 paints. The cache saves **98.8%**.
4. **Isolate clocks.** A 1Hz age clock mounted above a 65-row list re-renders 65 rows per
   second forever. Put it inside the one component that reads it, and give
   day-granularity countdowns a 60s clock, not a 1s one.
5. **`content-visibility: auto` on closed groups bought nothing measurable** and is
   marginally *worse* on open. Do not reach for it.
6. **One integrator vs per-row rAF made no measurable difference at this scale.** Keep the
   shared integrator for *correctness* (II.4.13, one clock) — do not claim it as a
   performance win. The cheapest sweep is neither: a rate-derived CSS transition, zero
   script per frame.
7. **Do not reach for virtualization.** Nothing measured justifies it.

**Two known foundation limits, recorded rather than hidden:**
- **Shed stage 1 is a no-op on slow-data screens** (no honest live process means no pulse
  to pause), and **stage 3 has no lever on `reveal`** — the accordion's own 260ms reveal
  is the most expensive motion on STORES and II.4.12 lists only peer, drill-in and tray.
  Either accept a two-rung ladder on these screens or extend stage 3 to cover `reveal`.
- **`src/cd/material/textures.ts` cannot be re-keyed to a world's palette.** It bakes
  neutral hexes in (`knurl` at `#34373D`/`#4A4E56`/`#2C2F35`) and offers no seam, which
  violates II.2's own "a language re-keys hue and value to its own palette without
  touching structure". Workaround until the factory takes palette keys:
  `background-blend-mode: overlay` over the world's own control token.

The shed ladder's trigger was repaired at `fe6a23e` — it had been measuring the interval
*between* frames as though it were the cost *of* one, and escalated to stage 3 on any
idle page within ~3s. If you are reading a pre-`fe6a23e` branch, that bug is live.

## Contrast-audit integrity — READ BEFORE YOU MEASURE ANYTHING

The STORES builder found this and it invalidates a measuring habit every builder on
this project has used:

> **A gradient surface declared with the `background` shorthand has
> `background-color: transparent`.** A contrast walker that reads
> `getComputedStyle(el).backgroundColor` therefore does NOT get the painted ground — it
> falls through to whatever ancestor happens to have a colour. Measured on STORES: two
> alert inks that read **4.98:1** against the walked ancestor were actually **3.63:1**
> and **3.91:1** against their real painted plate. **A Floor failure hiding inside a
> passing audit.**

`.cd-key`, `.cd-register-head` and the `Enclosure` variants in `src/cd/foundry/foundry.css`
are all declared this way (six surfaces). So:

1. **When you measure, composite the actual painted gradient** — take its **darkest
   stop** as the worst case — rather than trusting `backgroundColor`. Several screens
   have reported "0 contrast failures" using the naive walk; **those figures are
   suspect wherever the text sits on a gradient surface**, and the final review must
   re-sweep them.
2. **When you paint, declare `background-color` alongside `background-image`** at the
   gradient's darkest stop, so the declared case and the worst case are the same case.
   STORES does this throughout and it is the pattern to copy.
3. The foundry itself is **not yet repaired** — it was left alone deliberately while
   builders were mid-flight, since changing six shared surfaces under them was the worse
   risk. Repair it once the last screen lands, then re-run every screen's contrast sweep.

## Rulings from the final review (2026-08-18)

**The acknowledge verb is `ACK`, product-wide.** The review found four verbs for one act
— "ACK" on TODAY, "address →" on STORES, "ACT" on SHOP, "ack" + "act →" on LIST. Per-
language *voicing* is defensible, but the acknowledge act is the same act in every room,
and this product already rules that semantics are engineering rather than taste (the five
reserved roles are one hex each for exactly this reason). So: **one verb for the
acknowledge act everywhere; the NAVIGATIONAL key stays per-room** and keeps its own
language's voice. A room that both acknowledges and navigates prints both, in that order.

**Contrast figures must name the WORST STOP, not "on cream".** The measured-repairs table
below certifies the channel inks "on cream" without naming the plate's own darkest
gradient stop, and that omission is the contrast trap in miniature. Measured: the macro
ladder inks read 5.17+ against the flat token and **4.32 / 4.39 against the plate's
darkest stop `rgb(220,211,191)`** — passing where the glyphs sit, but with the margin
gone. Any contrast figure recorded anywhere in this project must state the ground it was
measured against, and that ground must be the worst stop the painted surface can present.

**Trophy Mode is one wall, not seven.** `useTrophy` exists in **seven** copies and they
are no longer byte-identical: today/plan/stores/meal are code-equal, shop/list differ in
formatting, and **COOK diverges semantically** — it wakes on `pointermove` and `focusin`
and clears its idle timer on `visibilitychange`, where the other six wake only on
pointerdown/keydown/wheel/touchstart. The consequence is lived, not theoretical: a dimmed
TODAY ignores an approaching hand while COOK wakes to one. **COOK's behaviour is the
correct one** — a wall that ignores an approaching hand reads as broken. Promote a single
`useTrophy` carrying COOK's wake list and visibility guard, and delete the other six.

## Reporting requirements

Every builder's final report must contain: the committed values used (hexes, springs, durations, sizes) with the chapter clause each came from · **your five-pass critique with every finding in defect–evidence–repair form, and what you repaired** · the §6.6 checklist, seven lines, each answered · confirmation you **rendered and looked at it**, with what you actually saw · measured contrast and target sizes · anything you could not meet, stated plainly (§6.5).
